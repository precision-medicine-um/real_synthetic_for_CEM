
dataset = '' # name of the dataset e.g. 'MUMC', 'GR'
subset = '' # subset e.g. 'train', 'test'

infodir = r'' # directory of the input file
input_file = '' # name of the Excel file with input data
sheetname = '' # name of the sheet in the Excel file to use

inputdatadir = r'' # directory of the input data
outputdatadir = r'' # directory of where to save the output data

startidx = 0 # inidex of starting row in the Excel sheet

viewlist = ['CC', 'MLO'] # list of mammographic views to process
preprocessing_method = 'mass' # preprocessing method, either 'mass' or 'calcification'


